spec Spec {

  is "The primitive that describes systems — three keywords compose any system: is, works when, subassemblies."

  works when {
    *.spec   parses at every node
    *.spec   exists at every node
  }

  subassemblies {
    Vocabulary
    Primitives
    Examples
  }
}
