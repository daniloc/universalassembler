spec Examples {

  is "Canonical patterns to pattern-match when writing new specs — each child describes a shape to study."

  works when {
  }

  subassemblies {
    MinimalLeaf
    WithChecks
    WithFrontier
  }
}
